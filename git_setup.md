git init
プロジェクトのはじめにやる

git status
今、どの状態か

git add
gitのステージに上げる（まだgithubにアップロードはされていない）

git add .←　これで、全部をステージに上げる

git commit -m""
自分のパソコンだけに変更を保存する

git push orgin main~
変更をサーバーへプッシュ（ローカルから全国へ展開）

あれをコピーしてターミナルで入力
(githubのURLを指定して、そこにpushする作業)

そしたらgithubを更新
これでpush完了、オープンソースで誰でもみることができる

setting→pages→mainから公開できたはず
しばらく待ってみる
これで公開されます。